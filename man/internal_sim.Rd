% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz_internal_sim_dat.R
\name{internal_sim}
\alias{internal_sim}
\alias{rkhs_norm}
\alias{rkhs_norm_mat}
\alias{cfbm_rkhs}
\alias{cfbm_rkhs_cross}
\title{Internal RKHS Norm Utilities}
\usage{
rkhs_norm(X1, X2 = NULL, Ginv, Index)

rkhs_norm_mat(X, Ginv, Index)

cfbm_rkhs(X, Ginv, Index, Hurst = 0.5)

cfbm_rkhs_cross(X_train, X_new, Ginv, Index, Hurst = 0.5)
}
\arguments{
\item{X1}{Sample matrix or vector}

\item{X2}{Sample matrix or vector}

\item{Ginv}{(Generalized) Inverse of the Gram matrix}

\item{Index}{Matrix specifying tensor vectorization indices}

\item{X}{Input data (list of samples or matrix where rows are samples)}

\item{Hurst}{Hurst coefficient in (0, 1]}

\item{X_train}{Training data (list or matrix)}

\item{X_new}{New data (list or matrix, same type as X_train)}
}
\value{
\code{rkhs_norm}: Numeric value representing the RKHS norm between X and Y

\code{rkhs_norm_mat}: Symmetric matrix of pairwise RKHS norms

\code{cfbm_rkhs}: Centered Gram matrix for fractional Brownian motion in RKHS

\code{cfbm_rkhs_cross}: Cross-centered covariance matrix
}
\description{
Internal functions for computing RKHS norms and related operations.
  Used internally by \code{sim_dat}. Not exported or intended for direct use.
}
\examples{
\dontrun{
G <- diag(1,4)
rkhs_norm(X1 = matrix(1:4, nrow=2),
          Ginv = solve(G),
          Index = expand.grid(1:2, 1:2))
}
\dontrun{
G <- diag(1,4)
rkhs_norm_mat(X = list(matrix(1:4,2), matrix(5:8,2)),
              Ginv = solve(G),
              Index = expand.grid(1:2, 1:2))
}
\dontrun{
G <- diag(1,4)
cfbm_rkhs(X = list(matrix(1:4,2), matrix(5:8,2)),
          Ginv = solve(G),
          Index = expand.grid(1:2, 1:2),
          Hurst = 0.7)
}
\dontrun{
train <- list(matrix(1:4,2), matrix(5:8,2))
new <- list(matrix(9:12,2))
G <- diag(1,4)
cfbm_rkhs_cross(X_train = train,
                X_new = new,
                Ginv = solve(G),
                Index = expand.grid(1:2, 1:2))
}
}
\keyword{internal}
