% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz_internal.R
\name{internal_utils}
\alias{internal_utils}
\alias{suffix}
\alias{parse_index}
\alias{create_index_matrix}
\title{Internal Utility Functions}
\usage{
suffix(id)

parse_index(X, string)

create_index_matrix(dat_T)
}
\arguments{
\item{id}{An integer or numeric value}

\item{X}{A tensor or array to subset}

\item{string}{Character string of comma-separated indices (e.g. "1,2,")}

\item{dat_T}{A list of numeric vectors, where each vector represents the time points
for a different variable.}
}
\value{
\code{suffix}: Character string with ordinal suffix appended (e.g. "1st", "2nd")

\code{parse_index}: Subsetted tensor/array

\code{create_index_matrix}: A numeric matrix where each row represents a unique combination of time points.
}
\description{
Internal helper functions for string and index processing.
  Not intended for direct use by package users.
}
\examples{
\dontrun{
suffix(1)   # "1st"
suffix(22)  # "22nd"
suffix(13)  # "13th"
}
\dontrun{
X <- array(1:8, dim = c(2,2,2))
parse_index(X, "1,2,")  # Returns X[1,2,]
parse_index(X, ",1")    # Returns X[,1]
}
\dontrun{
# Two variables
create_index_matrix(list(c(1, 2), c(3, 4, 5)))

# Three variables
create_index_matrix(list(c(1, 2), c(3, 4), seq(0,1, length.out = 4)))
}
}
\keyword{internal}
